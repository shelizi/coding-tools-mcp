[package]
name = "coding-tools-tunnel-protocol"
version = "0.2.0"
edition = "2021"
license = "Apache-2.0"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
